const sendNotification = async (userId, message) => {
  // Save notification or push notification logic
  console.log(`Notification sent to user ${userId}: ${message}`);
};

module.exports = {
  sendNotification
};